══════════════════════════════════════════════════════════════════
🚀 SOLUCIÓN RÁPIDA - AGREGAR SOPORTE PARA SALDO A FAVOR
══════════════════════════════════════════════════════════════════

⚠️ IMPORTANTE: Sin ejecutar este script, verás errores al:
   - Registrar pagos con excedente
   - Crear facturas para clientes con saldo a favor
   - Ver el saldo a favor de un cliente

📋 INSTRUCCIONES PASO A PASO:

1. Abre Supabase Dashboard:
   → https://supabase.com/dashboard

2. Selecciona tu proyecto

3. Ve al SQL Editor:
   → Menú izquierdo → "SQL Editor"
   → Haz clic en "+ New query"

4. Copia el código SQL:
   → Puedes copiar desde aquí abajo
   → O abre el archivo: ADD_CREDIT_BALANCE.sql

5. Pega en el SQL Editor de Supabase

6. Ejecuta:
   → Haz clic en "RUN" (botón verde)
   → O presiona Ctrl+Enter / Cmd+Enter

7. Verifica el resultado:
   → Debes ver mensajes en verde
   → Mensaje final: "✓ Columna credit_balance configurada correctamente"

8. Recarga tu aplicación:
   → Presiona F5 o Ctrl+R
   → Los errores deberían desaparecer
   → El sistema de saldo a favor estará funcionando

══════════════════════════════════════════════════════════════════
📄 CÓDIGO SQL - COPIAR DESDE AQUÍ ↓
══════════════════════════════════════════════════════════════════

-- Agregar columna credit_balance si no existe
DO $$ 
BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM information_schema.columns 
    WHERE table_name = 'clients' AND column_name = 'credit_balance'
  ) THEN
    ALTER TABLE clients ADD COLUMN credit_balance DECIMAL(10, 2) DEFAULT 0 NOT NULL;
    RAISE NOTICE 'Columna credit_balance agregada exitosamente';
  ELSE
    RAISE NOTICE 'Columna credit_balance ya existe';
  END IF;
END $$;

-- Asegurarse de que todos los registros existentes tengan un valor válido
UPDATE clients 
SET credit_balance = 0 
WHERE credit_balance IS NULL;

-- Crear índice para mejorar el rendimiento
CREATE INDEX IF NOT EXISTS idx_clients_credit_balance ON clients(credit_balance) 
WHERE credit_balance > 0;

-- Verificar que la columna se creó correctamente
DO $$
DECLARE
  column_exists BOOLEAN;
BEGIN
  SELECT EXISTS (
    SELECT 1 FROM information_schema.columns 
    WHERE table_name = 'clients' AND column_name = 'credit_balance'
  ) INTO column_exists;
  
  IF column_exists THEN
    RAISE NOTICE '✓ Columna credit_balance configurada correctamente';
    RAISE NOTICE '✓ Todos los clientes tienen saldo a favor inicializado en 0';
  ELSE
    RAISE EXCEPTION 'Error: No se pudo crear la columna credit_balance';
  END IF;
END $$;

══════════════════════════════════════════════════════════════════
📄 CÓDIGO SQL - COPIAR HASTA AQUÍ ↑
══════════════════════════════════════════════════════════════════

DESPUÉS DE EJECUTAR:
✓ Debes ver mensajes en verde
✓ Debes ver: "✓ Columna credit_balance configurada correctamente"
✓ Recarga tu aplicación (F5)
✓ Los errores deberían desaparecer

SI VES ERRORES:
- Verifica que estás en el proyecto correcto de Supabase
- Asegúrate de copiar TODO el código (desde DO $$ hasta el final)
- Contacta si el problema persiste